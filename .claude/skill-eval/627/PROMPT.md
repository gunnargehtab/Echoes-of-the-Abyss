Work issue #627 in this repository, end to end, on the branch you are already on.

Read the issue with the GitHub tools and implement **option A**, which is the
option the issue itself recommends. Both of its open questions are already
settled in the issue body: a contained exception is logged and dropped, and the
simulation-interval branch ends the room without announcing anything.

The issue's acceptance criteria are the definition of done. Treat all six as
binding, including the last one — `MatchRoom` has no test file today and needs
one.

Finish by running `npm run gates` and getting it to pass. Then commit and push
the branch.

Do not open a pull request, do not assign the issue, and do not comment on it.
This branch is not for review.
