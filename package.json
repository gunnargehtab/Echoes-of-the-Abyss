{
  "name": "echoes-of-the-abyss",
  "version": "0.0.1",
  "description": "Browser-native RTS with acoustic fog of war set in the Pelagion Rift",
  "type": "module",
  "workspaces": [
    "packages/shared",
    "packages/frontend",
    "packages/backend"
  ],
  "scripts": {
    "dev": "npm run build:shared && concurrently -n backend,frontend -c cyan,magenta \"npm -w packages/backend run dev\" \"npm -w packages/frontend run dev\"",
    "build:shared": "npm -w packages/shared run build",
    "build": "npm run build:shared && npm -w packages/frontend run build && npm -w packages/backend run build",
    "test": "npm run build:shared && npm -w packages/shared run test --if-present && npm -w packages/frontend run test --if-present && npm -w packages/backend run test --if-present",
    "balance": "node tools/balance/run.mjs",
    "type-check": "npm run build:shared && npm -w packages/shared run type-check && npm -w packages/frontend run type-check && npm -w packages/backend run type-check",
    "lint": "npm -w packages/shared run lint && npm -w packages/frontend run lint && npm -w packages/backend run lint",
    "format:check": "prettier --check \"packages/**/*.{ts,tsx,css,json}\" \"tools/**/*.{js,json}\" \"*.json\"",
    "format": "prettier --write \"packages/**/*.{ts,tsx,css,json}\" \"tools/**/*.{js,json}\" \"*.json\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "esbuild": "^0.19.0",
    "eslint-config-prettier": "^9.1.2",
    "prettier": "^3.9.6",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
