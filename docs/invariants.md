# Invariants

Properties this simulation must hold, where each one comes from, and the test that
holds it.

None of these are new. Every row below was already enforced before this document
existed — as an assertion in a suite, usually with a long comment above it saying what
the fault was and why the shape of the test is what it is. What was missing is a list:
somewhere a person, or an agent, can read what "correct" means here without first
knowing which of 182 test files to open.

## What belongs here, and what does not

An invariant is a property that must hold **over every input**, not an expected value
for one. "No entity exists above `world.maxEid`" is an invariant. "The Clarion costs 90
Alloy" is a number, and it lives in `packages/shared/src/constants.ts` with a SPEC tag
and a doc citation.

Three tests of whether a row belongs:

- **Breaking it is a bug, not a balance change.** A repriced hull breaks nothing; an
  entity above the bound is invisible to the Echo pass and to the Drift, silently.
- **It is held by a test, not by a convention.** A rule nobody checks is a comment.
  A row whose holder does not exist fails `npm run check:invariants` — that gate is
  what stops this document becoming the second description that drifts.
- **It states what must be true, not how.** The holder may be rewritten freely; the row
  survives it.

**This list is not complete and does not claim to be.** It states the invariants that
were written down as invariants by the tests that hold them. Adding a row is a normal
part of fixing a bug whose failure mode was silent — that is exactly the kind this
document is for.

## The invariants

Each row names the property, the doc section or issue it descends from, and the file
and test that hold it. `npm run check:invariants` verifies that every holder still
exists, by file and by test name.

**The numbers are ordinals, not identifiers.** Splitting or inserting a row renumbers
everything below it, and the gate reads only the table — it cannot see a cross-reference
elsewhere that still cites an old number. Refer to a row by what it says, not by its
number. Three references in this repository had to be repaired after one split for
exactly this reason.

| # | Invariant | Source | Held by |
| --- | --- | --- | --- |
| 1 | Every spawn path registers its entity, so a pass walking ids ascending under `world.maxEid` sees every entity that exists | bitECS sizes stores to capacity; the bound is only correct while this holds | `packages/backend/test/world.test.ts` — `entity id high-water mark` |
| 2 | Ordnance does not listen. A torpedo or mine never resolves as an observer for the player who fired it | [systems-combat.md](systems-combat.md) §6 — a mine waits to hear you, it does not report | `packages/backend/test/ordnanceFog.test.ts` — `ordnance and the fog of war` |
| 3 | The own-ordnance payload carries no detection the player has not made — no `locked` flag, no target identity | Server-authoritative (`CLAUDE.md`) | `packages/backend/test/ordnanceFog.test.ts` — `ordnance and the fog of war` |
| 4 | A spawn writes **every** field of its component, because bitECS hands entity ids back with the old bytes still under them | #617; [systems-combat.md](systems-combat.md) §5 — a decoy a committed seeker ignores | `packages/backend/test/ordnanceSpawn.test.ts` — `spawnOrdnance against a recycled id (#617)` |
| 5 | Every spawn and its Foundry stands on ground some region *shapes* — a region covering the point sets `floorM` or `ceilingM` — on every map, mission maps included, with one named exemption that a second test makes expire | [maps.md](maps.md), "How a map is written"; #622, strengthened by #636 | `packages/backend/test/maps.test.ts` — `paints the ground under every base it seats, on every map`; `packages/backend/test/maps.test.ts` — `keeps no exemption it has stopped needing` |
| 6 | Every spawn and its Foundry on a catalogue map sits over water deep enough to hold a structure | [systems-depth.md](systems-depth.md) §2; `CONSTRUCTION.WORKING_DEPTH_M` | `packages/backend/test/maps.test.ts` — `seats every spawn and its Foundry over deep enough water` |
| 7 | A mission beat never fails an objective the player has already met — objective status is monotonic | [mission-tolerance.md](mission-tolerance.md) §13 — "the runtime's own monotonicity invariant" | `packages/backend/test/missionUnderworks.test.ts` — `the choice, as rules` |
| 8 | The mission panel prints the progress counters the server sent it and computes none of its own | [ui-ux.md](ui-ux.md) §10.5; the `INVARIANT:` comment in `packages/shared/src/missions.ts` | `packages/frontend/test/missionPanel.test.ts` — `shows the counters the server sent and does no arithmetic of its own` |
| 9 | The Echo pass stays inside its work budget, counted as path integrals rather than timed | `packages/backend/CLAUDE.md`, "Two clocks"; `SIM.ECHO_BUDGET_MS` | `packages/backend/test/match.test.ts` — `stays inside its work budget for a small match in contact` |
| 10 | The 60 Hz step stays inside its work budget, counted as pair tests and cell probes rather than timed | `packages/backend/CLAUDE.md`, "Two clocks"; `sim/stepWork.ts` | `packages/backend/test/separation.test.ts` — `stays inside the 60 Hz per-tick work budget with a crowd` |
| 11 | `MAX_UNIT_RADIUS_M` bounds every hull in the roster, so the broadphase cannot miss a pair | Derived from the longest hull in `UNIT_STATS`; #149, #461 | `packages/shared/test/units.test.ts` — `roster invariants` |
| 12 | Every navy has a hull at every rung, so no commander is left with nothing to save for | [roster-plan.md](roster-plan.md) §4, Wave 0; #518 | `packages/backend/test/aiRung.test.ts` — `has a rung hull for every navy, so no navy is left with nothing to save for` |
| 13 | No propagation cell exceeds the grid's reported peak, and that peak never exceeds the loudest PF anything is specified to produce | [environments.md](environments.md); #372 — a storm ×10 is a modifier like any other | `packages/backend/test/hazards.test.ts` — `resonance storms`; `packages/backend/test/terrainChange.test.ts` — `the water a beat rewrites` |
| 14 | The five block reasons of the commander's ordnance want partition it exactly — every observation that reaches the want increments one, so they sum to the observations counted | #698 — the reading a report takes off this table is a share, and a share of a miscount is wrong in a way nothing else catches | `packages/backend/test/balance.test.ts` — `partitions the ordnance want into the reason it was stopped` |
| 15 | The relief shade only ever darkens an authored fill, scales with the size of the ground step rather than saturating, and moves luminance without moving hue | [graphics-standards.md](graphics-standards.md); [style-neon-noir.md](style-neon-noir.md) — the shading is a lighting model, checked against the light rather than a screenshot | `packages/frontend/test/palette.test.ts` — `reliefShade` |
| 16 | The shell sends no **movement** order for a hull the mission is holding — move and harvest, the two a pointer reaches directly — and arms no attack-move over a selection it is holding whole, on the `W` key; it reads a selection as held only when **every** hull in it is, so a mixed selection still goes | [ui-ux.md](ui-ux.md) §10.5 — the lock is continuous state, learned before the press, and an order to a mixed selection still moves what is not held; #478, #708, #722. Three verbs rather than the seven this row used to name, and the difference is what is *driven* rather than what is written: all seven shell paths do ask `movable` or `heldSelection`, but attack-contact, embark, dive and follow-floor are reached in no test, and #722's rule for this doc is that a row states what a test holds. The ENGAGE button carries the same refusal as the key and is named here for the same reason it is not claimed: nothing drives it either. The refusal that binds regardless is the server's — the row below, which holds every order that starts a hull moving. Orders the hold does not refuse (stop, hold, throttle, silent, disembark) are deliberately outside this row | `packages/frontend/test/movementHolds.test.ts` — `a selection the mission holds whole says so before the press`; `packages/frontend/test/rendererSmoke.test.ts` — `sends no order at all for a hull the mission is holding`; `packages/frontend/test/rendererSmoke.test.ts` — `arms no attack-move on a selection the mission is holding whole` |
| 17 | Every order that moves a hull asks the mission's hold first — move, attack-move, attack-contact, embark, dive, follow-floor and harvest: every order that **starts a hull moving**, whether it writes the order itself or sets state a 60 Hz system turns into one | [mission-sorrowgate.md](mission-sorrowgate.md) §8 — a held hull "stops where it stands"; #478, #708, #722. The clamp runs at 5 Hz and movement at 60, so a path that skips the refusal is not merely late, it is unbounded. Stated by **effect** rather than by what an order writes, because the writing criterion this row used to give would have excluded both paths #719 found: `orderHarvest` writes only `Harvester.mode` and `orderAttackContact` only `Weapon.orderedTargetEid`, and `harvest.ts` and `combat.ts` turn each into movement a tick later. A row that is the checklist before adding an order may not exclude the shape of order that caused the bug | `packages/backend/test/missionHold.test.ts` — `a harvest order is a movement order, and the hold refuses it too`; `packages/backend/test/missionHold.test.ts` — `an ordered target is a movement order, and the hold refuses it too`; `packages/backend/test/missionHold.test.ts` — `the order path and the wire agree about what is held`; `packages/backend/test/missionRuntime.test.ts` — `refuses the order while the hull is held, whatever the player clicks`; `packages/backend/test/missionHold.test.ts` — `refuses follow-floor, because a hold that let a hull drift down a slope is not one`; `packages/backend/test/missionHold.test.ts` — `refuses an attack-move from that same hull, with nothing added and nothing unlocked`; `packages/backend/test/missionHold.test.ts` — `refuses attack-move, and a hull with guns takes the same order once it is free`; `packages/backend/test/missionHold.test.ts` — `leaves a held hull in the water, and lets the same hull board once it is free` |
| 18 | The conn probe drops a frame interval only when the page went hidden across it — never for its size — so a stall the player watched is always in the station's worst case | [graphics-standards.md](graphics-standards.md) gate 6; #286 — the first real-GPU drive lost a three-second stall to a 500 ms guard | `packages/frontend/test/rendererSmoke.test.ts` — `prices a stall the page stayed visible through, and drops only a hidden gap (#286)` |
| 19 | A hull the commander claims as a tender is ordered loud and into the Shelf band, and is handed back silent if the army it rejoins is silent, because claiming it removes it from every other branch that would have ordered either | [economy.md](economy.md) §6 — bloom-share is the Commune's income, and `bloomShare.ts` pays only a tender that is in the band and not silent; #706 | `packages/backend/test/aiFlora.test.ts` — `lifts Silent Running off the hull it sends gardening`; `packages/backend/test/aiFlora.test.ts` — `brings a tender hanging under the rim up into the Shelf band`; `packages/backend/test/aiFlora.test.ts` — `hands a released tender back silent when the army it rejoins is silent`; `packages/backend/test/aiFlora.test.ts` — `does not silence a released tender back into a loud army` |
| 20 | Every path from master to the output ceiling passes through the speaker profile's input, so a profile that exists is a profile the device hears whether it is switched on or off | [audio-direction.md](audio-direction.md) §11, §12; #663, #681 — a profile off the output path is the same defect as no profile, and every reading taken on the profiled path says nothing is wrong | `packages/frontend/test/speakerProfile.test.ts` — `leaves no path from master to the device that goes around the profile` |
| 21 | The figure a mission's silence order reads out to the player and the figure `applySilenceLedger` charges against are one number over one id set, and a mission that keeps no ledger reads out nothing at all | [mission-sorrowgate.md](mission-sorrowgate.md) §4 — the order binds one authored role and not the whole force, so the SIG meter beside the panel measures a set the rule does not bind; #623 criterion 8 | `packages/backend/test/silenceReadout.test.ts` — `publishes a reading in every mission that keeps a ledger, and in no other`; `packages/backend/test/silenceReadout.test.ts` — `reads a narrower set than the meter beside it, and never a wider one`; `packages/backend/test/silenceReadout.test.ts` — `crosses at the tick the ledger charges, both ways` |
| 22 | A navy's nodule bank rises by no more than its depots took in, and by exactly that much wherever nothing is bought — the Order by both of its §6 terms rather than either | [economy.md](economy.md) §6; #706 — only the deposit (`systems/harvest.ts`) and the tithe (`systems/tithe.ts`) credit nodules, so what the holds delivered and what the account rose by are one ledger reached from two ends. The equality is exact only where nothing is spent, because `nodulesEarned` is a per-observation stockpile delta and a purchase landing in the same pass as a deposit nets against it; under a commander what survives is the half spending cannot break, that a bank never outruns its depots | `packages/backend/test/balance.test.ts` — `balances the nodule ledger exactly when nothing is spent (#706)`; `packages/backend/test/balance.test.ts` — `keeps the nodule trip inside its own bounds under a commander (#706)` |
| 23 | A readout's explanation on the permanent strip covers **its own number and no other**, and says what that number currently says: a control is refused where the strip has laid the readout off the canvas or on top of another, and the surface republishes whenever any figure the line quotes moves, including the ones its own drawn text does not carry | [ui-ux.md](ui-ux.md) §2, §7, §11; #724 — both halves failed silently and neither was reachable by a gate. A collision test over the controls already *accepted* is green while a control sits on a readout the canvas bound had just refused, because a refused readout is still drawn; and the SIG instrument is two lines whose box is named by the first, so `4 units · 2 loud` becoming `5 units · 2 loud` moved nothing the change detector was watching and the line under it went on quoting the old count | `packages/frontend/test/rendererSmoke.test.ts` — `never lays a control over a number that is not its own`; `packages/frontend/test/rendererSmoke.test.ts` — `republishes when §3’s second line moves under a held peak`; `packages/frontend/test/rendererSmoke.test.ts` — `quotes the same loud count as the label the line sits under`; `packages/frontend/test/rendererSmoke.test.ts` — `keeps the permanent element at every scale, and every box on the canvas` |
| 24 | The plain gloss a mission shows beside an objective's reading is one of that objective's own authored strings — chosen, never assembled — names no proper noun belonging to a party that is not the player's — except a navy name the player's own party also flies — and describes the reading the row is currently showing | [ui-ux.md](ui-ux.md) §10.5's gloss rule; #720's decision, #725. The rule that binds a reading binds the line explaining it, and interpolation is the only way a resolved contact could reach either: a gloss fixed before the match exists cannot carry a figure about anything, which is what makes the anti-reveal rule here a property of the shape rather than of the author's care. Those are three claims held by three different things, and keeping them apart is the point: the match cannot reach the field because nothing interpolates into it, an author cannot *name* somebody else's force because the authored strings are swept against every foreign tag, navy, spawned species, hull- or structure-kind word and **capitalised** speaker word the mission seats, minus the words the player's own kinds use, matched from the start of a word so a name in the plural is the same name (§10.5 lets a gloss name the player's own hulls: the Commune fields Light Scouts too, and a bare substring match refused the word *flight*, while anchoring both ends of it stopped catching *Corvettes*); and the two halves of a row cannot describe different states because `glossFor` pairs a gloss to the reading that is live and `missions.test.ts` refuses any alternate reading authored without one — [mission-sorrowgate.md](mission-sorrowgate.md) §12 authors two readings of the silence order, and a row whose sentence said one and whose gloss explained the other would mislead in the half the player reads for the facts. **The second clause is a necessary condition, not the whole anti-reveal rule, and this row under-claims on purpose.** A deny-list over proper nouns cannot see *"three other delegations hold the east and the west"*, which names nobody and gives away a count and two bearings; and the named exception is real rather than theoretical — Sorrowgate seats a Commune delegation in the water beside a Commune player, so *Commune* cannot be banned without banning the player's own navy. §10.5's "never a count of anything hostile" is held by review. A row claiming otherwise is how a reveal ships on the strength of a green gate, which is what three rounds of review on #725 established | `packages/backend/test/objectiveGloss.test.ts` — `sends only strings the mission authored, chosen and never assembled`; `packages/backend/test/objectiveGloss.test.ts` — `swaps to the debt gloss exactly when the court’s reading swaps, and back`; `packages/backend/test/missions.test.ts` — `glosses a rule in the player’s own terms and names nobody else’s hulls`; `packages/frontend/test/missionPanel.test.ts` — `prints the plain gloss beside the authored line and never instead of it`; `packages/frontend/test/missionPanel.test.ts` — `keeps the authored line first, and the gloss after it and inside the same row` |
| 25 | A contact's committed audio events never outlive the tier that earned them: a voice's mechanism is scheduled ahead of the caller, and everything still standing is taken back the moment the family changes, or the voice stops — on those two and on no other event | [audio-direction.md](audio-direction.md) §3 ("Tier 2 must not carry class information in its timbre"), §8.1, §12's scaffold status; #731 — a scheduling horizon is not an exemption from a tier. Events are placed up to `EVENT_HORIZON_S` ahead so §8.1's faster families can be rendered at their own rate at all, which means a contact demoted below Tier 3 has clicks already on the graph: the horizon less one tick, and every click the swarm fits into it. The failure mode is silent in exactly the way this list exists for — the mix goes on naming a navy the server has stopped naming, for a tenth of a second, and nothing in the frame or the log disagrees. Held across eight demotion phases rather than one, because whether a click *happens* to fall inside the committed window depends on where the train's phase is when the tier changes, and a single phase would assert this on luck | `packages/frontend/test/contactTimbre.test.ts` — `takes back a family’s committed events when the family changes, and only then`; `packages/frontend/test/contactTimbre.test.ts` — `takes a stopped voice’s committed clicks down with it` |
| 26 | A voice's own mechanism state never carries across a family it was not sounding: the reciprocating cycle's phase resets with the event train on every family change, so a contact reopens on the loaded stroke and never mid-cycle | [audio-direction.md](audio-direction.md) §8 ("the only faction with a *beat*"), §3 ("the return of a sound that was dying is itself a warning"); #731. Row 25's property one step inward. That row takes back the events already *on the graph* when a family changes; this one takes back the counter that decides what the next event will *be*. A Consortium contact flickering across the Tier-2 boundary is a mechanism change and back, which is ordinary rather than exotic, and without the reset it reopens on the return — the short half, 1.80 dB quieter — at the instant §3 wants a refreshed contact to be a warning in its own right. Silent in the way this list exists for: the mechanism is still the Consortium's, the beat is still exact, and only the first strike after the gap is the wrong half of the cycle. Held across eight demotion phases for the reason row 25 gives — the train is 0.4167 s against a 0.2 s tick, so only some phases leave the counter odd, and the first holder asserts at least one of them did. **The two halves are held by different tests, because the gate here is liveness only and cannot say otherwise.** The first holds *reopens as it opened*, reading the level off the voice's own opening strike rather than a written-down one; that the opening is the loaded stroke is the other two, on the envelope of the first strike and on the alternating lifts. A voice that opened and reopened on the return satisfies the first alone | `packages/frontend/test/contactTimbre.test.ts` — `reopens a Consortium contact on the loaded stroke after it falls and returns`; `packages/frontend/test/contactTimbre.test.ts` — `gives the swarm a tick, the Consortium both, and every other family the breath`; `packages/frontend/test/contactTimbre.test.ts` — `gives the Consortium a stroke and a return, and spends nothing on when` |
| 27 | The word a mission's silence order is named by on screen is authored prose or is absent, and is never the role the ledger indexes that set by | [ui-ux.md](ui-ux.md) §10.5 — the set-name says *which* hulls the reading is over, and `MissionRole` is a free string of per-mission internal ids, so a role rendered directly gives `called SIG 022 / 025`; #623 criterion 9 | `packages/backend/test/silenceReadout.test.ts` — `names the bound set from the authored word alone, or not at all`; `packages/frontend/test/missionPanel.test.ts` — `names the bound set where the mission worded it, and not otherwise` |
| 28 | A garden claim the commander makes is withdrawn from a tender that is still on its way to it only when the hull leaves the army — dead, aboard a carrier, embarking, or designated scout — or when the bed leaves the briefing: the gate decides whether to start a claim and whether to keep one that has **arrived**, and never whether to keep one that is walking | [economy.md](economy.md) §6 — bloom-share pays a hull standing in a bed, so a claim that does not survive the walk pays nothing at all; #706 and the decision on it. Row 19 puts a *claimed* tender in the state the share is paid for; this row is about the claim lasting long enough for that to matter. The failure mode was silent for months because the commander's own bookkeeping is the only place it shows: measured on `ventfront-divide`, seeds 4000-4002, 16 of 17 claims ended with the hull holding them still alive and five reached a bed. Nothing in a balance report distinguishes a tender that was never sent from one that was sent and recalled, which is why no column ever caught it. **The two clauses are held by different tests and the arrived half is the older one**, in the way row 26 keeps its halves apart: the walking half is the two tests written for this row, whose fixtures stand the army at its spawn; the arrived half is row 19's release test, whose fixture stands it on the garden, and which passes either side of this change because the gate over an arrived tender is exactly what did not move | `packages/backend/test/aiFlora.test.ts` — `holds a tender that is still walking when the army dips under the gate`; `packages/backend/test/aiFlora.test.ts` — `keeps sending that tender to the one bed it was sent to`; `packages/backend/test/aiFlora.test.ts` — `hands a released tender back silent when the army it rejoins is silent` |
| 29 | A hull the Sorrowgate silence order binds never exceeds that order's ceiling by moving: movement is two states and nothing between them, so a bound hull reads one figure under way at every speed and that figure is under the ceiling | [mission-sorrowgate.md](mission-sorrowgate.md) §4 clause 1 and §9; [systems-echo.md](systems-echo.md) §2; the decision on #741. §2 used to carry a "Moving (flank/boost) 45-70" band that `acoustics.ts` has never modelled, and §4 derived the prologue's first lesson from it — a Light Scout at flank "far above the ceiling" of 20. The decision deleted the band. The failure mode this row guards is the band coming back by a different door: a speed term added to the movement branch would make §4's "moving never puts a hull of the flight over the ceiling" false without anyone editing the mission, and a bound hull whose cruise figure rose over 20 would do it without anyone editing `acoustics.ts`. **Scoped to this mission and not to the registry, which is the unusual part of this row.** The general form is false as the campaign stands — `attending-attendance` binds a hull that cruises at 28 against a ceiling of 25 — so the ceiling is a lever elsewhere and a reading only here; the holder file's last test pins that scope so nobody widens the rest of it | `packages/backend/test/silenceUnderWay.test.ts` — `binds hulls whose every movement figure is under the ceiling`; `packages/backend/test/silenceUnderWay.test.ts` — `reads one figure under way, at every speed, and it is under the ceiling`; `packages/backend/test/silenceUnderWay.test.ts` — `is not vacuous: descent puts the same hull over the ceiling`; `packages/backend/test/silenceUnderWay.test.ts` — `finds a ceiling in the registry that a bound hull does cross by moving` |
| 30 | The two readouts the permanent strip may drop are measured against the row they are **drawn on**: the map name and the clock are gated on the first row's own right edge — the draw meter's last segment, not the `DRAW` label it runs past — so they yield rather than print over the stockpile row | [ui-ux.md](ui-ux.md) §13 (a 52 px strip holding two rows) and §2 (which readouts it carries — §2's own diagram draws them on one line, so the y the split happens at is `EchoRenderer.drawHud`'s rather than the doc's); #743. The rule read `exposureLabel` or `bandLabel`, both at y = 30, while everything it gates is at y = 10 — a row neither drop can collide with — so its answer carried no information about the row that could actually collide, and across §13's sweep it found room at every scale right through 200%. Silent in the way this list exists for: nothing throws, every gate stays green, and the failure is two numbers printed over each other at a UI scale §11 makes a correctness requirement rather than a preference. It survived because the second row is short exactly when the first is full, so no fixture at 100% shows it, and because the right edge that matters belongs to `Graphics` segments rather than to the `Text` beside them. Held over the *rendered* strip rather than over the predicate, and at a scale where the rows disagree: a rule reading the second row cannot tell the two frames of the first holder apart | `packages/frontend/test/rendererSmoke.test.ts` — `drops the clock on the row it is on, not the row below it`; `packages/frontend/test/rendererSmoke.test.ts` — `never prints the two droppable readouts over another number, across §11’s range` |
| 31 | An objective row's **border box** is no wider than the panel body it sits in, in both shapes a row can take — so the progress counter in its third column is never drawn outside the panel | [ui-ux.md](ui-ux.md) §10.5, whose closing paragraph makes the counter content rather than decoration ("an objective may say *get both tenders out*"), and §11, which makes the 75–200% scale a correctness requirement; #752. `.objectives-row` set `width: 100%` beside `padding: 5px 8px` and no `box-sizing`, so the row's border box was the body's width plus 16 px. Silent in the way this list exists for, and for a reason worth writing down: a row that names a marker is a `button`, which the UA stylesheet already gives `border-box`, so **only the markerless `p` rows overflowed** — two shapes side by side in one panel, under identical author CSS, boxing differently. No test could see it either, because `react-test-renderer` lays nothing out and jsdom has no layout engine, which is why the holder reads the shipped stylesheet and does the box arithmetic instead of measuring. That reader is itself a thing that can quietly stop guarding — a rule it drops or a property it ignores reports a box narrower than the browser's — so it throws on whatever it cannot resolve, and its own failure behaviour is held by `packages/frontend/test/cssBox.test.ts`. Held over every row shape rather than one mission, and at three body widths, because the fault is a property of the rule and not of the viewport it was first seen at | `packages/frontend/test/missionPanel.test.ts` — `boxes every row shape inside the body, so no progress counter is cut`; `packages/frontend/test/missionPanel.test.ts` — `boxes the two host tags identically, which is what hid the fault`; `packages/frontend/test/missionPanel.test.ts` — `does not depend on the UI scale, at either end of §11’s 75–200%` |
| 32 | The permanent strip's refusal sees a whole instrument rather than the half of it that is `Text`: the draw meter's segmented bar is inside the `DRAW` readout's own box, so a readout the bar prints through is refused a control exactly as one printed through by a number is | [ui-ux.md](ui-ux.md) §13 and [economy.md](economy.md) §2 — the draw is the one resource that is never banked and the bar is how the strip says so, which makes it half of that readout the way §3's meter is half of the SIG one; #757. `recordStrip` reads the `Text` objects the strip has just laid out, which is what stops it from keeping a second copy of the layout and is also why, on its own, it can only ever record a *number*. The bar is `Graphics` and is the last thing on the first row, so nothing recorded it and the comparison had a blind spot the length of it: the contact count kept a control over glyphs the segments print through — the surface wrong about a number rather than silent about it, which is the one thing §13 promises it will not be. Silent in the way this list exists for, and doubly so: nothing throws, every gate stays green, and the test that walks drawn glyphs cannot see it either, because ink is not a glyph | `packages/frontend/test/rendererSmoke.test.ts` — `refuses a control to a readout the draw meter’s bar prints through` |
| 33 | An objective row's **grid** cannot be pushed wider than the row: the track carrying the mission's own sentence and its gloss has a floor that is a length rather than the contents of the track, and every child of the row can shrink to it | [ui-ux.md](ui-ux.md) §10.5, whose authored text is rendered verbatim and is therefore unbounded — a mission's words, not a template's — and §11, which makes the 75–200% scale a correctness requirement; #760. The row is the same one as row 31 and the fault is not: #752 fixed the row's **border box**, and the grid inside that box is a separate axis it never touched. `grid-template-columns` was `3.2rem 1fr auto` with no `min-width` on the children, and a `1fr` track's automatic minimum is min-content, so one long unbreakable token in an authored objective takes the middle track to that token's own width. **The row's border box never moves** — `width: 100%` and `border-box` pin it to the body — so the symptom is not a wider row but the columns to the right of that track being carried out of the panel with it. Driven on `attending-the-dome` at 1440×900, 100% UI scale, with one such token written into the objective text: the middle track resolves to 412.75px without the two declarations against 218.69px with them, and the progress counter's *layout* box sits at x 567 against a panel edge of 381. It is not drawn there: `.objectives-body` sets `overflow-y: auto`, and an `overflow` that is `visible` on one axis computes to `auto` on the other, so the body is a scroll container on both axes and clips at its padding box — checked in Chromium rather than read off the spec: `overflowX` computes to `auto` and `scrollLeft` moves. So the counter is not spilled over the world; it is scrolled out of a box nothing teaches the player to scroll, which is [ui-ux.md](ui-ux.md) §2's "invisible rather than absent, and the player cannot tell the difference" — the state §2 rejects a scrollbar for, reached without one. Row 31's "cut every `n of m`" by the other axis. Latent rather than observed, which is why it had to be driven with an injected token rather than found: the longest unbreakable run in any authored objective or gloss is 14 characters (`Undermarshalcy`), so every shipping row wraps with the whole track to spare. What the pair does **not** do is stop that token's own glyphs printing across the cells beside it: the ink runs to about x 521, over the whole of the counter's column, so in any row where the token shares a line with the counter it prints straight over it. That is a third axis — how a word may be broken — owed against [ui-ux.md](ui-ux.md) §2 rather than against taste; it was #774 and it is row 35. `.contact-log-row` has carried the same two declarations since its own columns overran each other, which is what makes this a row that was missed rather than a rule nobody had; `.mission-result-objective` carried the same shape without it until #773, which is row 34; `.mission-log-row` carries the shape and not this exposure, and that is driven rather than reasoned: its `1fr` is the *last* column and `.mission-log-text` spans `1 / -1`, and the row's tracks are **identical with and without an injected token** — at 100% UI scale and at 200% — so the spanning item's contribution moves neither track, and no cell is carried anywhere. The authored line's own ink does leave `.mission-log-entries`, which computes `overflow-x: auto`: 186px of scroll in Chromium at 1440×900 and 100% UI scale against `MissionLog`'s own markup with one such token, 361px at 200%. That residual is not this rule's. Held over every row shape rather than one mission, and against the placement the stylesheet actually declares, so an edit moving the authored text to another column moves the check with it rather than leaving it guarding an empty one | `packages/frontend/test/missionPanel.test.ts` — `gives a mission’s own words a track that cannot widen the row`; `packages/frontend/test/missionPanel.test.ts` — `lets every child of a row shrink to its track, which the track alone does not`; `packages/frontend/test/cssBox.test.ts` — `reproduces #760 when the two declarations are taken away` |
| 34 | The **result card's** objective row holds the same property as row 33, over all six row shapes the card can draw — each of the three status words with a progress counter and without one, the counter being independent of the status: the track carrying the mission's own sentence is floored by a length rather than by its contents, and every child of the row can shrink to its track — so an authored objective cannot carry the `n of m` beside it out of the card | [ui-ux.md](ui-ux.md) §10.5, whose authored text is rendered verbatim and is therefore unbounded, and whose closing paragraph makes the counter content rather than decoration — the same clause row 31 cites for it — with [mission-sorrowgate.md](mission-sorrowgate.md) §8 for why the card is a reading of the run rather than a score; #773. Row 33 fixed `.objectives-row`, and that was one of the three places in this stylesheet with the shape: `.mission-result-objective` was `3.2rem 1fr auto` with no `min-width` on its children, so the same long unbreakable token in the same authored sentence floored the middle track at the token's width. Driven in Chromium at 1440x900 against the card's own markup and the shipped sheet: the middle track resolves to **597.69px** without the two declarations against **455.83px** with them, and the counter's layout box runs to a right edge of x 1141.86 from a left of 1104.88, against a padding edge of 1025.59 — **116px outside the card**. Not drawn there, for row 33's reason one panel along: `.mission-result-panel` sets `overflow-y: auto` and an `overflow` that is `visible` on one axis computes to `auto` on the other, so the card is a scroll container both ways and the counter is scrolled out of a box nothing teaches the player to scroll — [ui-ux.md](ui-ux.md) §2's "invisible rather than absent". Latent in the same way and for the same reason: the longest unbreakable run in any authored objective is 14 characters, so every shipping row wraps with the track to spare. Which track carries the sentence is read off flow order here rather than off a `grid-column`, because this row declares none — so the holder asserts that too, and a child given an explicit column fails it rather than moving what it guards. What the pair does **not** do is stop the token's own glyphs printing across the cells beside it: 71px of the card's horizontal scroll survives the fix as ink rather than as boxes, which is #774's axis on this row; it was #809 and it is row 38 | `packages/frontend/test/resultCards.test.ts` — `gives a mission’s own words a track that cannot widen the row`; `packages/frontend/test/resultCards.test.ts` — `lets every child of a result row shrink to its track`; `packages/frontend/test/cssBox.test.ts` — `reproduces #773 when the two declarations are taken away` |
| 35 | An objective row's **ink** stays off the counter beside it: the two cells a mission authors into may break a word they cannot otherwise fit, and the two the client templates — the status word and the counter — may not, in any shape the panel's rows take | [ui-ux.md](ui-ux.md) §2, which requires this panel to **fit what it holds** and rejects hiding what does not, because that makes it "invisible rather than absent, and the player cannot tell the difference"; §10.5's closing paragraph for why the counter is content rather than decoration, and §11 for the 75–200% scale; #774. Rows 31 and 33 are the same row's other two axes and neither reaches this one, which is the fact worth carrying: **a box is not ink.** Both of those are about how wide a box is, and a word with no break opportunity in it is laid out on one line whatever its box measures — so flooring the track stopped the counter being *carried* out of the panel and did nothing about its being *covered* where it sits. Driven in Chromium at 1440×900 against the panel's own markup and the shipped sheet, with a 64-character token written into an authored objective: the token's glyphs cover **457 px²** of the counter's own glyphs at 100% UI scale, **257** at 75% and **970** at 200%, and **134 px** of ink leaves the body's padding edge at 100%. Every one of those is **0** with the declaration in, the body's horizontal scroll with it. The comparison is ink against ink rather than ink against the counter's box, and that distinction is the measurement's own correction rather than a nicety: the counter is a grid item at `grid-row: 1` and stretches the whole row, so a token on a *later* line intersects its box without covering a digit — which is exactly #774's own reading of #760's screenshot, where `1 of 2` and `0 of 3` were legible and `4 of 3` was not. **Both halves of the property are load-bearing, and the second half is what the first cost when it was written too wide.** The declaration went in on `.objectives-row > *` first, beside row 33's `min-width`, and from there it also reached `.objectives-status`, whose `font-size` is a flat `0.58rem` while its track is `calc(3.2rem * var(--panel-type, 1))` and *shrinks* above 100% — so from about 119% the word stopped fitting and broke where it used to overflow its track intact: `PENDIN` / `G` at 120%, `FAILE` / `D` at 165%, **on shipping content with no token in it**, across the range §11 makes a correctness requirement. That also cost the panel a whole objective row at 150% on Sorrowgate's four real objectives (33 px of vertical scroll and 3 of 4 rows whole, against 1 px and 4 of 4 on `main`), which §2 does not allow either. Scoped to `.objectives-text, .objectives-gloss` the vertical figures are identical to `main` at 100%, 150% and 200%. Latent in row 33's way — the longest unbreakable run in any authored objective or gloss is 14 characters (`Undermarshalcy`) — so the fault had to be driven with an injected token rather than found; the regression above needed no token at all, and passed 756 green frontend tests, which is why every cell in a row is now classified **and asserted** rather than counted. `break-word` rather than `anywhere`: measured here the two are indistinguishable — identical line counts, row heights and scroll at 75%, 100% and 200% — and the reason is narrower than "no track is content-sized", which is false, the third track being `auto`. It is that both cells this rule reaches sit in the `minmax(0, …)` track, whose minimum is a length rather than its contents, so the min-content contribution the two values differ in cannot reach a track from here; the narrower declaration is therefore preferred rather than needed. **What this rule does not do** is stop the *status* word's ink reaching the sentence beside it at the top of §11's range: that word overflows into the gap up to about 165% and prints 95.6 px² of `PENDING` over `Get` at 200%. The figures are identical against `main`, so it is a fourth axis this declaration neither causes nor closes | `packages/frontend/test/missionPanel.test.ts` — `breaks a word a mission authors too long for its cell, so the counter still reads`; `packages/frontend/test/cssBox.test.ts` — `reads the row’s cells as they now ship`; `packages/frontend/test/cssBox.test.ts` — `reproduces #774 when the declaration is taken away` |
| 36 | An entry nothing but the command card reaches is never what yields when the card is offered more than its twelve cells hold | [ui-ux.md](ui-ux.md) §9's yield order, and §2, which makes the card "how a touchscreen reaches any order at all"; #815. The card sliced its model to twelve and what went was whatever the model pushed *last* — a property of the authoring order rather than of what an entry costs to lose. On every hull carrying torpedoes, which is ten of them and the Corvette and the Cruiser among them, a readout of the torpedo count held the twelfth cell and the depth charge fell off the end. Silent in the way this list exists for: the slice drew a full, tidy grid, `C` still laid a charge on a keyboard, and nothing in the frame or the log said an order was missing. A keyboard was also the old comment's own justification for the slice — "every entry on this card also has a binding" — which was false for production and beside the point for touch, where there are no bindings at all. Held on the Corvette because that is the hull a player selects first and the one the fault fired on | `packages/frontend/test/rendererSmoke.test.ts` — `keeps every order a torpedo hull can give, and moves the count to the stat line` |
| 37 | A swarm's organise cycle is the cohort's own rather than the clock's: the phase is walked along the voice's own event train, by each event's own interval, and reset with that train — so where in the cycle a cohort stands follows from when it was classified, and the cycle keeps one rate in wall clock at every freshness | [audio-direction.md](audio-direction.md) §8 ("clicks that phase into unison as cohorts converge — you hear them *organise*"), §3 for the decay that lengthens a fading contact's period; #731 and the decision on it. Row 26's property for the other compound family, and the question #742 left open: `strokePhase` was already counted along the train precisely so that two Consortium contacts are not on one crank, while `swarmSpread` read absolute time — the same number for every voice holding it, so every cohort in the water closed and scattered on the same instant however it got there. §8 makes a cohort "many small things agreeing", one contact's worth of them, so what converges is a cohort and not the ocean. Silent in the way this list exists for: the mechanism is still the Directorate's, the rate is still §8.1's, and what a player hears is the tell of a synthesizer rather than of a swarm — two voices 77 ms apart tracking each other's spread to 3 ms. **What a per-train phase does not give, and this row must not claim**: the separation between two cohorts is Δt₀ × `SWARM_ORGANISE_HZ` modulo 1, so it *aliases*. Two classified one Echo tick apart stand 0.038 of a cycle apart and track each other's spread to 4.41 ms, and **the residual has no positive floor**: 0.2 s × 0.19 Hz is 19/500, and 19 is coprime with 500, so the separation at k ticks is (19k mod 500)/500 and it reaches every multiple of 1/500. It is 1.40 ms at 26 ticks, 0.23 ms at 79 — 15.8 s apart, an ordinary separation inside one match — and exactly zero at 500, so two cohorts classified 100 s apart are in step. The 3 ms the decision names as the tell is crossed and not approached. That is inherent to option 2 rather than a fault in this transcription of it, and it is recorded rather than chased: the decision took this shape as the prerequisite for driving the spread off *actual* convergence, which is where the residual goes. **The three halves are held by different tests**, in the way row 26 keeps its halves apart, because they fail to different mistakes and a gate that is liveness only cannot say so. The first is driven at half a cycle and at no smaller separation — voices a few clicks apart look alike under either rule, so a fixture standing them close together passes on the code this replaces — and it carries its own control: two cohorts classified on the same tick still land identical clicks, so the phase is a function of the train rather than a seed. The second is the reset, which nothing else reaches: the phase does not advance under the thump, so without it a cohort that flickers across the Tier-3 boundary reopens where it fell, measured at 0.0653 s of spread against 0.0759 s at the fall. Its fixture falls half a cycle in, where the cluster is widest, for the reason row 26 spends a `sawMidCycle` guard on. The third holds the rate: the phase advances by each event's own interval, decay included, so a cycle counted in *events* runs at 6.37 s against 5.26 s on that holder's own fixture, whose freshness ramps from 1 to 0.05 across the run — §3's decay choosing the rate rather than §8 | `packages/frontend/test/contactTimbre.test.ts` — `gives each cohort its own organise cycle, so two swarms are not one swarm`; `packages/frontend/test/contactTimbre.test.ts` — `reopens a cohort gathered after it falls below Tier 3 and returns`; `packages/frontend/test/contactTimbre.test.ts` — `organises at its own rate whether the contact is fresh or fading` |
| 38 | The **result card's** objective row keeps its **ink** off the counter beside it, as row 35 holds for the panel's: the one cell a mission authors into may break a word it cannot otherwise fit, and the two the client templates — the status word and the counter — may not, in every one of the six row shapes row 34 enumerates | [ui-ux.md](ui-ux.md) §2, which requires the card to **fit what it holds** and rejects hiding what does not, because that makes it “invisible rather than absent, and the player cannot tell the difference”; §10.5, whose authored text is rendered verbatim and is therefore unbounded, and whose closing paragraph makes the counter content rather than decoration; #809. Row 34 is this row's other axis and says in its own last clause what it does not reach — **a box is not ink** — so flooring the track stopped the counter being *carried* out of the card and did nothing about its being *covered* where it sits. What makes this a fourth instance rather than row 35 restated is why it outlived #774: **the fixture that caught the panel fits here.** The card's cell is **455.8 px** at 1440×900 against the panel's 218.7, and its type is `0.64rem`, so #774's 64-character token inks **394.3 px** and never reaches the counter at all — 0 ink on it, 0 scroll, with the declaration and without — and that control ported straight across passes against a sheet it never changed. (#809's body reports 431.5 px for that token; re-measured here it is 394.3, and the other three figures reproduce to the decimal. The reading is the same either way, both being inside the 455.8 px cell.) A control here needs a longer token: at **80** characters the token's glyphs cover **349.3 px²** of the counter's own, and at **100**, **443.8 px²** and **90 px** of the card's horizontal scroll, which `.mission-result-panel` has for row 34's reason — `overflow-y: auto` computes the other axis to `auto`. Every one of those is **0** with `.mission-result-text { overflow-wrap: break-word; }` in. Latent in row 34's way and for its reason: the longest unbreakable run in any authored objective is 14 characters, so the fault has to be driven with an injected token rather than found. **Scoped to the authored cell rather than to the row's items**, which is #774's correction carried across — but the driver differs and the difference is worth recording rather than inheriting: the panel's status word broke because its track is `calc(3.2rem * var(--panel-type, 1))` and *shrinks* above 100% while its `0.58rem` does not, and **this card's track is a flat `3.2rem` against a flat `0.58rem`**, with no `--panel-type` and no `zoom` reaching it. So the scoping is owed here to the argument `.mission-result-progress` already wins with `white-space: nowrap` — a templated word in a fixed track must not break, `4 of 3` over three lines being the same illegibility one column along — rather than to a measured regression. Both templated cells are therefore **asserted to refuse**, not merely left alone, which is what stops the declaration being written on the row's item rule again. `break-word` rather than `anywhere`, for row 35's reason: the cell sits in the `minmax(0, …)` track, whose minimum is a length rather than its contents, so the min-content contribution the two values differ in cannot reach a track from here. **What this rule does not reach** is the card's *other* authored cell, and rows 33 and 35 each name their residual where this one named none: the epilogue is a `p` directly under the panel rather than a cell of this row, so neither this declaration nor row 34's floors touch it, and it carried no rule of its own. That is #829 and it is row 41 — where the fix is **not** this one. `break-word` is inert on that element, because it is a shrink-to-fit flex item whose width is floored by its own min-content rather than by a track; the value that works here is the one that would not work there | `packages/frontend/test/resultCards.test.ts` — `breaks a word a mission authors too long for its cell, so the counter still reads`; `packages/frontend/test/cssBox.test.ts` — `reads the result row’s cells as they now ship`; `packages/frontend/test/cssBox.test.ts` — `reproduces #809 when the declaration is taken away` |
| 39 | A craft never outlives the carrier that launched it: on the tick a carrier dies, every craft in its flight dies with it, and the list of them dies with the id | [systems-combat.md](systems-combat.md) §15 — "guidance is the deck's"; #838. The hold's rule at the other end of the same argument, and its failure mode is the one this list exists for: an orphaned craft is a hull nobody is flying, still heard, still shooting, holding a `Craft.carrier` that bitECS will eventually hand to something else — at which point the orphan belongs to a stranger's deck. It is silent because nothing in a report distinguishes a craft from any other small hull | `packages/backend/test/flight.test.ts` — `dies with it, on the tick it dies` |
| 40 | A deck never holds more than its capacity, counted across both states at once — what is aboard and what is in the water | [systems-combat.md](systems-combat.md) §15; [economy.md](economy.md) §10 — a carrier's berths are three for the hull and one for every craft its deck can hold, charged when the carrier is queued. The two facts are one fact: the population cap bounds what is in the water only while a deck cannot exceed the number its berths paid for, so a rebuild clock that counted the shed alone would put a second flight's worth of hulls on the board for nothing | `packages/backend/test/flight.test.ts` — `opens on an enemy inside the tether and not before`; `packages/shared/test/units.test.ts` — `charges the flight to the carrier’s berths, three and one a craft` |
| 41 | The result card's **epilogue** keeps its own ink inside the card: the one cell a mission authors into at panel level may break a word it cannot otherwise fit, and the three the client templates — the outcome heading in each of its three shapes, the action row and the hint under it — may not | [ui-ux.md](ui-ux.md) §2, which requires the card to **fit what it holds** and rejects hiding what does not, because that makes it “invisible rather than absent, and the player cannot tell the difference”; §10.5, whose authored text is rendered verbatim and is therefore unbounded — a mission's words, not a template's; #829. Rows 34 and 38 are the objective row's two axes and **neither reaches this element**: both stop at the row, and the epilogue is a sibling of the list rather than a cell in it. What makes this a fifth instance rather than row 38 restated is that **the fix that works one element down is inert here.** Row 38's cell sits in a `minmax(0, …)` track whose minimum is a length, so `overflow-wrap: break-word` and `anywhere` are indistinguishable there and the narrower value is preferred. This element sits in no track: `.mission-result-panel` is `display: flex; flex-direction: column; align-items: center`, so the epilogue's width is shrink-to-fit and **is** floored by its own min-content contribution — which `break-word` leaves alone. The box grows to the token and the token never has to break. Driven in Chromium against the shipped sheet and `MissionResult`'s own rendered markup, with a token injected into Thin Water's Partial epilogue. At **1440×900** the panel's content box is **559.8 px** and the paragraph's box reaches **573.1 px** at 80 characters (**7 px** of card scroll) and **716.3 px** at 100 (**78 px**). The shell also runs on a phone ([SETUP-ANDROID.md](../SETUP-ANDROID.md)) and §2 asks the panel to fit at every width: at **360 px** the content box is **306.8 px** and the fault fires at 64 characters already — 458.5 px and **76 px** of scroll, rising to **205 px** at 100. **`break-word` reproduces every one of those figures to the decimal**, which is why it is recorded as inert rather than as weaker; with `anywhere` the box clamps to the content box and the scroll is 0. The card's horizontal scroll exists for row 34's reason — `overflow-y: auto` computes the other axis to `auto` — and the overrun splits **either side** because the panel centres its items, so half of it is off the left edge where no scrollbar reaches it at all. Read at 75%, 100% and 200% and identical at all three, at both widths: neither `--ui-scale` nor `--panel-type` reaches this card, which is row 38's own observation about its tracks one element down, so §11's scale range is one reading here rather than three and the regression row 35 had to correct has no driver. **The face has to be read before any of those figures is trusted**, and this is the trap that cost this row its first draft: `--font-data` is declared in `index.css` rather than in `App.css`, so a harness loading the stylesheet under test alone lays the card out in **Times New Roman** — a proportional card no player sees. Every figure captured that way is wrong, it under-reports the overrun (it calls an 80-character token harmless), and it invents a “widest glyph” reading that cannot occur, the shipped face being `ui-monospace, Consolas, monospace` where every advance is equal. The harness asserts the computed `font-family` before it reports. Latent in row 34's way and for its reason: the longest unbreakable run in the **87** authored epilogue readings across 29 missions is **14** characters (`Undermarshalcy`) — the same token rows 35 and 38 name for the objective cells — so the fault has to be driven with an injected token, and the shipping text is untouched by the declaration at either width. **All four templated shapes are asserted to refuse**, not merely left alone, which is what stops the declaration being written on `.mission-result-panel > *`: the heading is walked in each of `OUTCOME_CLASS`'s three classes, because a walk of one outcome asserts one of them and row 38's convention is to enumerate every shape. The hint paragraph under the buttons is a fifth child the first draft of that walk missed, which is the silence the classification refuses | `packages/frontend/test/resultCards.test.ts` — `breaks a word an epilogue authors too long for the card, and nothing templated`; `packages/frontend/test/cssBox.test.ts` — `reads the card’s epilogue as it now ships`; `packages/frontend/test/cssBox.test.ts` — `reproduces #829 when the declaration is taken away` |

## Why a gate, and what it does not do

`npm run check:invariants` reads the table above and asserts that every holder resolves:
the file exists, and the test name appears in it. A row naming a test somebody renamed
or deleted fails loudly, in the same idiom as `npm run check:models` and the roadmap's
drift report.

That is a **liveness** check, not a correctness one, and the distinction matters. The
gate cannot tell you the test still asserts what the row claims — only that something by
that name is still there. What it buys is the failure mode this document would otherwise
have: a list that reads as authoritative while quietly describing a suite that has moved
on. Prose that repeats what code does drifts; a script makes the drift loud.

The tests themselves are what hold the invariants, and they run in `npm test` like
everything else. This gate only holds the *list*.

## Related

- `CLAUDE.md` — constants in one place, server-authoritative · `packages/backend/CLAUDE.md` — the two clocks
- [maps.md](maps.md) — how a map is written, and the three authoring faults its tests caught
- [systems-combat.md](systems-combat.md) — ordnance, decoys, and what a mine does
- [systems-echo.md](systems-echo.md) — detection, the pass the Echo-budget row bounds
- [ui-ux.md](ui-ux.md) — §10.5's status region, whose counters the mission-panel row holds
- [README.md](README.md) — the rest of the design bible
